# Legacy Source Cleanup

Updated: 2026-09-15, source checkpoint verified and commit/sync authorized
State: VERIFIED (commit/sync authorized; inspect Git history for final outcome)

## Latest Request
- User: "please handle the commit and sync carefully". Commit the curated index
  and sync main with origin/main normally; no force push or history rewriting.
- Pre-commit recheck: 213 staged paths, artifact/hash audit PASS; origin/main is
  up to date after fetch. Common staged credential-marker scan found no matches;
  this is not a comprehensive security or licensing certification.
- Preserve ignored/untracked data, local archives and running services. Do not add
  the remaining local reports. Prior staging-only restrictions below are historical.

## Objective And Authority
- User approved Stock Research retirement, a standalone Persistence page, and
  the legacy daily study harness (explicitly KEEP the optional ridge observer).
- Follow-up approved removal of Dashboard and ticker scanner-history details.
- Latest clarification: copy the ENTIRE page as-is before editing the existing
  working file. Complete Dashboard/TickerDetail originals were copied first and
  remain unchanged; current working pages remain, minus retired functionality.
- Initial retirement scope excluded permanent archive deletion, commits and runtime
  work; later explicit staging, startup and commit authorizations are recorded here.
- Earlier user: "yes, its moved". Repository listing confirms legacy absent.
  Approved completion of current tests/build without it; no recreation, relocation,
  permission changes or service restarts. External destination not supplied.
- [Dependency inventory and hold list](../LEGACY_CLEANUP_AUDIT.md) is authoritative.
- Latest authorization via clarification: create a NEW root legacy folder and
  copy full files as-is, then keep needed functionality in working files. Ignore
  all backend/backups AND untrack its files, preserving every disk file.
- Earlier readiness review did not authorize staging or startup. Later requests
  explicitly authorized the V2 cutover and the source staging described below.

## Authorized Staging
- Latest request: stage files needed in the repository, excluding local artifacts.
  Backup entries marked D are intentional index removals, not new backup content.
- User confirmed: archive complete reappeared legacy sources, restore retirement,
  then stage those source deletions. Ten full originals, 151,777 bytes, verified in
  local legacy/restored-retirement-2026-09-15/manifest.json before active removal.
- Staged 213 paths: 130 additions, 62 modifications, 21 deletions. Fourteen deletions
  untrack backups; all fourteen local files remain. No tracked backup/archive files.
- Included source, tests, migrations, guidance, readable docs, frozen study configs,
  sample manifests and required runtime inputs. Three ridge enrollment reports are
  deliberate inputs: challenger, price diagnostic and price replication JSON.
- Those three reports use narrow -text attributes with CRLF-aware whitespace checks.
  Staged blobs equal local bytes; source SHA-256 and freeze_manifest validation pass
  using staged inputs in a disposable directory (two disjoint 300-ticker samples).
  No observer enrollment, fitting, provider requests or database writes performed.
- Ignored: backend/backups, root legacy and the 148,120,483-byte scorecard JSON.
  Thirty-five other generated reports/CSV/reference PDF remain local and UNSTAGED,
  not blanket-ignored. No data, original archive bytes or frozen report bytes changed.
- Staged present content is about 11.7 MB; largest blob is the 7,296,735-byte ridge
  input. No environment files, database files, caches or large generated output added.
- Checks: staged-index audit and git diff --cached --check PASS; retained backend
  task 304 PASS; frontend model/navigation/Persistence 69 PASS; TypeScript clean.
  Earlier 647-test scope included 27 opt-in database skips; no new migration audit.
- Local verifier: legacy/verify_staged_checkpoint.py (read-only without flags).
  At staging completion no commit or push had run; the latest request now authorizes
  both. Service restarts, data repair, replay and strategy promotion remain excluded.

## Subsequent Authorized V2 Startup
- User next authorized V2 worker+UIcutover and retaining all backupfiles locally.
  That supersedes the pre-cutover no-start finding below; see
  [current runtime checkpoint](stock-alert-review.md#current-v2-cutover).
- Launcher explicitlyquality2;defaultSHADOWreaderV2;386member new enrollment
  atSep15T08:54:46Z,warmupcomplete08:58:16Z,API200quality_v2,total0,awaiting10:15ET.
  Five equity-side workers running as resident terminals;API/Vite reused.
  No options/research startup or new context policy. Firstmarketcyclepending.
- Full4source/test copies archived legacy/v2-shadow-cutover-2026-09-15,
  55,900bytesverified;117cutovertestsPASS. V1view+SQLite2files unchangedhashes.
- Current index again contained14backupfiles;approveduntracking reapplied,0tracked,
  all14localfiles unchangedhashes. Commit/pull portability caveats in README.
  Source/configs stillversioned;no datapurge or sourcecommit performed.

## Earlier Pre-Cutover Review
- [Readiness findings](../LEGACY_CLEANUP_AUDIT.md#pre-commit-review-september-15):
 31changed/new backend test files647PASS,27opt-in PostgreSQL skips,6subtestsPASS;
 frontend69PASS,TypeScript/VitePASS. Changed-scope check, not entire repo/live data.
- Launcher tests13failed solely from host-wrapped text; full test original copied
 to legacy/commit-readiness-2026-09-15 with SHA256manifest, then wrapper captures
 raw warning/exception messages.39launcherPASS and full scopePASS. No real launcher
 behavior or assertions relaxed. Local test/process tasks updated, no extra workers.
- The initial index contained only fourteen backup deletions. The authorized staging
  above supersedes that state; the oversized and bulk generated reports stay local.
- Source/test/config/human-readable plan selective checkpoint reasonable; NOT a
 blanketcommit, licensed-content/secret certification or production qualification.
- Runtime approximately08:48UTC: API/Vite present; no stock-idea/ingestion worker.
 DefaultV2status NOT_ENROLLED; onlyV1state directory; no reader override in inspected
 terminal/.env allowlist. Live API200 source_idstock_ideas_forward_shadow_v1,
 as_ofSep14T21:49:57.714664Z,lastMISSED_PUBLICATION,next_publication_atnull.
- At that earlier review V2 was not enrolled; the later authorized startup above
  supersedes it. No autonomous scheduler audited. A retained READY view does not
  establish current uptime. Old xsmom/discovery and V1 code/evidence remain.
- [Post-commit plan](../STOCK_ALERT_CONTEXT_ENHANCEMENT_DESIGN.md#post-commit-delivery-contract)
 baseline/readiness/parity ->fixedV2/momentum/random/xsmom ->incrementalcontext
 ->prospectivegate ->coordinatedpage migration. No performance claims or new trials.

## Verified State
- New [root archive](../../legacy/README.md):8full originals/288,401bytes, before/after
  hashes verified. Earlier external archive untouched; local-absence notes below
  describe the earlier completed check, not the latest root state.
- Working discovery modules trimmed to shared features/filter/constants and snapshot
  reader. Retired capture/transition/paper marking logic/tests preserved in archive.
- Frontend service retains current Alerts only; unused old clients/types removed.
- Working price_diagnostic retains exact original causal scorer; observer import
  unchanged. Closed experiments/tests archived. Generic alpha CLI kept minus closed
  price-diagnostic flags. No historical replay, fit or observer startup performed.
- .gitignore whole backend/backups rule;14tracked backup files removed from INDEX
  with git rm --cached, zero tracked left, all14disk hashes unchanged. Staged Git
  deletions are intentional untracking, not data deletion. No commit.
- Current engine is implemented, unlike old September 13 summaries. Quality V2
  defaults in the forward CLI and SHADOW reader; retained V1 evidence is unchanged.
- Four batches documented in the [audit](../LEGACY_CLEANUP_AUDIT.md#external-archive): first three-file batch,
  stock-research-retirement (11 entries), legacy-daily-harness (5), scanner-history-
  retirement (7). Total26 full-file entries/777,051bytes, including shared copies.
- Retired UI: Stock Research, Dashboard scanner evidence, ticker Scanner History.
  All six scanner-history HTTP routes removed. Sector-performance endpoint retained.
- Persistence at /stocks/persistence reuses existing five-session published streaks;
  no normalized persistence feature store, screener/alert gate or migration added.
  Old research/backtest links redirect there; old ticker scanner link -> Overview.
- Current Alerts imports old-named stylesheet/service/API; current models import
  strategy_v2; canonical materialization imports old screeners/composites/patterns.
- Legacy daily batch/replay/merge and exclusive test archived. Read-only samples,
  completion and source-hash checks extracted into research/frozen_daily_study.py;
  retained scorecard/matched-control callers no longer import retired launchers.
- Daily V2 runner still needs matched controls/scorecard/strict outcomes; shared
  scripts and tests stay. Historical preparation/cache used by bootstrap, adjusted
  ingestion and ridge stays. Source hash drift still rejects old-run reuse.
- equity/scanner_research.py and dedicated tests now wholly retired after callers
  removed; earlier notes that Dashboard/ticker require them are superseded.
- Data, frozen studies, raw recovery media, migrations and unrelated user changes
  remain in place. Staging is not a full security, licensing or production review.

## Changes And Checks
- Before final staging:303current backend suitePASS;26scorer/observer/discoveryPASS(overlap),
  scorer/CLI9PASS,frontend69PASS,TypeScript/VitePASS,8archivecopiesVERIFY.
- Source trims used AST-selected ORIGINAL function bodies after full copying,
  followed immediately by focused tests. Manual test/client/CLI edits used patch.
- Old price-diagnostic task/command examples are historical; trimmed current module
  is not a runnable copy of the closed study. Full originals support restoration.
- Whole-file bytes/hashes pass explicit portable `manage_archive.py --verify-all`:
  26entries/777,051bytes. Routine frontend tests no longer read the archive.
- `source_path` accepts archive_root or STOCK_SCREENER_LEGACY_ARCHIVE_ROOT (absolute
  all-batches directory). Explicit bad locations never fall back. Current source
  remains authoritative; original expected hash checks unchanged. No live dependency.
- Portable verifier uses its own directory and needs only Python stdlib. No archived
  source/manifests modified. Shared copy policy remains immutable.
- After actual move: process task Check application without legacy archive now runs
  15 retained test files directly:306PASS/14warnings/26.66s. Includes current engine,
  V2/replay, screening, portal, schema, source verifier, launcher and ridge observer.
- Initial run305pass/1failure: PowerShell wrapped the expected error-message text.
  Normalized whitespace in test_start_workers.py assertion; same full task thenPASS.
  Failure exit/no-publication checks unchanged; no runtime launcher edit.
- After actual move: frontend69PASS; TypeScript and Vite buildPASS. No archive
  recreated. Removed five obsolete verifier/staging tasks referencing moved helper;
  unrelated tasks preserved. Archive pytest copies remain excluded by pytest.ini.
- Earlier browser (pre-relocation): Persistence loaded5sessions Sep8-14, AAPL filter1row. Desktop1440/mobile390
  no DOM body overflow; table scrolls internally. Screenshot tool returned stale
  frame, so mobile screenshot/pointer certification not claimed.
- Old Research -> Persistence; old /ticker/AAPL/scanner -> Overview with five tabs.
  Dashboard scanner section absent; no scanner-history requests in those views.
- Existing API/Vite reused; no workers started/stopped and no test execution pending.
- Earlier automatic rename was blocked by WinError5. Superseded by user's manual
  move; do not retry old helper task. No external-copy verification performed.

## File-By-File Review
- [Per-file inventory](../LEGACY_CLEANUP_AUDIT.md#file-by-file-usage-review) covers
  all234unique current changed/new/deleted non-backup Git paths. Read-only Git/list
  and Markdown table-target comparison: zero missing rows. Generated backup shards,
  ignored caches and unchanged files beyond relevant callers are explicitly excluded.
- Static dependency evidence, not runtime profiling or every-function certification.
- historical_actions validator used live via repository.persist_observation;
  historical_prices currently manual audit only. stock_discovery stock_features
  used by current Screener; old service producer functions are mixed with readers.
- price_diagnostic scorer still used by retained ridge; rest mostly closed studies.
  Old scorecard/matched-control/outcome tools still imported by daily V2 study runner.
- Repairs have test-only dependencies: test_signal_scorecard imports storage probe/
  reconstruction; test_screening imports source-gap repair helpers. Split tests/tools
  before archive. No whole remaining changed frontend page found unrouted; old
  getStockScreener/getStockAlerts client exports have no other current-source matches.
- docs/stock_idea_pilot_config.json is read by forward_config at runtime; NOTunused.
  Most other docs JSON/CSV are historical inputs/results, not current app inputs.
- No source/tests moved, no runtime tests/backtests rerun for this documentation audit.

## Next Step
- Select and stage source-only checkpoint, excluding oversized raw evidence and
 preserving required configs/samples. No commit created. Separately decide V2
 shadow cutover before relying on another market session; no existing activation.
- User can back up the NEW root legacy batch and run copied helper --verify.
  No direct app/test dependency on that archive. Operational-tool/test separation
  remains a separate batch; do not remove their mixed imports without review.
- Backup data is now untracked/ignored, so Git is not its backup. Keep current
  forward views/state and frozen study evidence on disk and separately backed up.
- Local absence validation complete. User retains/verifies external archive using
  copied helper --verify-all; destination unknown and external checksum not checked.
- For exact historical audits set STOCK_SCREENER_LEGACY_ARCHIVE_ROOT to the external
  all-batches root. No env destination invented/saved. Current app needs no archive.
- No more retirement approval needed for completed scope. Context enhancement
  Stage1 readiness is a separate future task, not started here.
- User selects next split/archive batch from the per-file audit; do not treat
  non-APP as automatic deletion permission. Preserve observer, current configs,
  shared helpers and all frozen evidence until its retention bundle is approved.
- Routine archive test dependency is removed; moved-source documentation links now
  point to the audit's external-archive instructions. Full-page rollback
  must preserve subsequent concurrent changes, not overwrite blindly.
- Do not delete shared V2/sector/persistence foundations, ridge observer, studies
  or V1 evidence. Worktree as a whole is not certified commit-ready.